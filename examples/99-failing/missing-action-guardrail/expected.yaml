# This example MUST fail. CI asserts the failure, not the success.
case: missing-action-guardrail
artifact: agent.yaml
kind: agent
expect:
  exit_code: 2
  finding: AA-SCH-001
  reason: >-
    The `action` guardrail point is absent entirely. An empty list is a recorded decision;
    a missing key is an oversight, so the schema requires all three points. Invariant 14.
