# This example MUST fail. CI asserts the failure, not the success.
case: unowned-agent
artifact: agent.yaml
kind: agent
expect:
  exit_code: 2
  finding: AA-SCH-001
  reason: >-
    owner.accountable is empty. A queue or blank field is not accountable; invariant 5
    requires a named person, because an agent with no owner has no one to answer for it.
