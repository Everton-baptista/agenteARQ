"""agentarch — an open standard for building AI agents.

This package ships the compiled CLI. There is no Python API: agentarch reads artifacts and never
enters an agent's execution path, which is what keeps it usable from any language.
"""

__version__ = "0.0.0"
