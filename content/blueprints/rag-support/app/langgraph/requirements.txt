# Pinned, for the same reason the model is: a floating dependency changes behaviour with
# nothing in the repository to review.
anthropic==0.69.0
pyyaml==6.0.2
langgraph==0.6.7
langchain-anthropic==0.3.21
